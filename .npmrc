# Security audit baseline (issue #20).
# Report vulnerabilities of "moderate" severity and above when running `npm audit`.
audit-level=moderate

# Keep caret/range specifiers when installing (matches existing package.json style).
save-exact=false
